<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Plans du Clan - Guerriers du Givre</title>
    <link rel="icon" type="image/png" href="/img/logo.png">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* (Même CSS que plans.ejs, tu peux le mettre dans style.css si tu veux factoriser) */
        .plans-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px; padding: 30px; }
        .plan-card { background: rgba(12, 36, 61, 0.8); border: 1px solid #e67e22; /* Orange pour clan */ border-radius: 10px; padding: 20px; transition: all 0.2s; cursor: pointer; text-decoration: none; display: flex; flex-direction: column; position: relative; }
        .plan-card:hover { transform: translateY(-5px); box-shadow: 0 5px 15px rgba(230, 126, 34, 0.3); background: rgba(12, 36, 61, 0.95); }
        .plan-title { font-size: 1.2rem; font-weight: bold; color: white; margin-bottom: 10px; }
        .plan-race { color: #f1c40f; font-weight: bold; text-transform: uppercase; font-size: 0.9rem; margin-bottom: 5px; display: flex; align-items: center; gap: 10px; }
        .plan-meta { color: #aaa; font-size: 0.85rem; margin-top: auto; padding-top: 15px; border-top: 1px solid rgba(255,255,255,0.1); display: flex; justify-content: space-between; }
        .lvl-badge { background: #f39c12; color: black; padding: 2px 8px; border-radius: 5px; font-weight: bold; font-size: 0.8rem; }
    </style>
</head>
<body class="hub-body">

    <header class="hub-header">
        <div class="header-overlay">
            <div class="header-content">
                <div class="user-info">
                    <h1><%= pseudo %></h1>
                    <p class="status"><strong><%= role %></strong> des Guerriers du Givre depuis <%= daysMember %> jours</p>
                </div>
                <div class="nav-menu">
                    <a href="/dashboard" class="nav-btn" title="Accueil"><i class="fas fa-home"></i></a>
                    <a href="/dinozs" class="nav-btn" title="Mes Dinozs"><i class="fas fa-paw"></i></a>
                    <a href="/clan" class="nav-btn disabled" title="Mon Clan"><i class="fas fa-chess-rook"></i></a>
                    <a href="/arbres" class="nav-btn" title="Plans"><i class="fas fa-scroll"></i></a>
                    <a href="/simulation" class="nav-btn" title="Simulation"><i class="fas fa-flask"></i></a>
                </div>
            </div>
        </div>
    </header>

    <div style="padding: 20px 40px; display: flex; justify-content: space-between; align-items: center;">
        <h2 style="color: #f7f7f7; margin: 0;"><i class="fas fa-users"></i> Plans du Clan</h2>
        <a href="/plans" class="btn-ice">Voir mes plans</a>
    </div>

    <div class="filters-container" style="padding: 0 40px; margin-bottom: 20px; display: flex; flex-wrap: wrap; gap: 20px; align-items: flex-end;">
        <div class="filter-group">
            <label style="color: #8b949e; font-size: 0.9em; font-weight: bold; margin-bottom: 5px; display: block;">Recherche (Nom) :</label>
            <input type="text" id="filter-name" placeholder="Nom du plan..." style="padding: 8px 12px; background: #0d1117; color: white; border: 1px solid #30363d; border-radius: 6px;" oninput="filterPlans()">
        </div>
        <div class="filter-group">
            <label style="color: #8b949e; font-size: 0.9em; font-weight: bold; margin-bottom: 5px; display: block;">Race :</label>
            <select id="filter-race" style="padding: 8px 12px; background: #0d1117; color: white; border: 1px solid #30363d; border-radius: 6px; min-width: 150px;" onchange="filterPlans()">
                <option value="all">Toutes</option>
            </select>
        </div>
        <div class="filter-group">
            <label style="color: #8b949e; font-size: 0.9em; font-weight: bold; margin-bottom: 5px; display: block;">Niveau :</label>
            <div style="display: flex; gap: 5px; align-items: center;">
                <input type="number" id="filter-level-min" value="1" min="1" max="80" style="width: 65px; padding: 8px; background: #0d1117; color: white; border: 1px solid #30363d; border-radius: 6px;" oninput="filterPlans()">
                <span style="color: #8b949e;">à</span>
                <input type="number" id="filter-level-max" value="80" min="1" max="80" style="width: 65px; padding: 8px; background: #0d1117; color: white; border: 1px solid #30363d; border-radius: 6px;" oninput="filterPlans()">
            </div>
        </div>
        <div class="filter-group">
            <label style="color: #8b949e; font-size: 0.9em; font-weight: bold; margin-bottom: 5px; display: block;">Joueur :</label>
            <select id="filter-player" style="padding: 8px 12px; background: #0d1117; color: white; border: 1px solid #30363d; border-radius: 6px; min-width: 150px;" onchange="filterPlans()">
                <option value="all">Tous</option>
            </select>
        </div>
    </div>

    <div class="plans-grid">
        <% plans.forEach(plan => { %>
            <a href="/plan/<%= plan.id %>" class="plan-card" data-name="<%= plan.name.toLowerCase() %>" data-race="<%= plan.race %>" data-level="<%= plan.level %>" data-author="<%= plan.author.pseudo %>">
                <div class="plan-race">
                    <i class="fas fa-dna"></i> <%= plan.race %>
                </div>
                <div class="plan-title"><%= plan.name %></div>
                <div class="plan-meta">
                    <span><i class="fas fa-user"></i> <%= plan.author.pseudo %></span>
                    <span class="lvl-badge">Niv. <%= plan.level %></span>
                </div>
            </a>
        <% }) %>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const cards = document.querySelectorAll('.plan-card');
            const races = new Set();
            const players = new Set();
            
            cards.forEach(c => {
                if (c.dataset.race) races.add(c.dataset.race);
                if (c.dataset.author) players.add(c.dataset.author);
            });
            
            const raceSelect = document.getElementById('filter-race');
            [...races].sort().forEach(r => {
                const opt = document.createElement('option');
                opt.value = r;
                opt.textContent = r;
                raceSelect.appendChild(opt);
            });
            
            const playerSelect = document.getElementById('filter-player');
            [...players].sort((a, b) => a.localeCompare(b)).forEach(p => {
                const opt = document.createElement('option');
                opt.value = p;
                opt.textContent = p;
                playerSelect.appendChild(opt);
            });
        });

        function filterPlans() {
            const nameSearch = document.getElementById('filter-name').value.toLowerCase();
            const raceFilter = document.getElementById('filter-race').value;
            const minLevel = parseInt(document.getElementById('filter-level-min').value) || 1;
            const maxLevel = parseInt(document.getElementById('filter-level-max').value) || 80;
            const playerFilter = document.getElementById('filter-player').value;
            
            const cards = document.querySelectorAll('.plan-card');
            cards.forEach(c => {
                const cName = c.dataset.name;
                const cRace = c.dataset.race;
                const cLevel = parseInt(c.dataset.level);
                const cAuthor = c.dataset.author;
                
                let show = true;
                if (nameSearch && !cName.includes(nameSearch)) show = false;
                if (raceFilter !== 'all' && cRace !== raceFilter) show = false;
                if (cLevel < minLevel || cLevel > maxLevel) show = false;
                if (playerFilter !== 'all' && cAuthor !== playerFilter) show = false;
                
                c.style.display = show ? 'flex' : 'none';
            });
        }
    </script>
</body>
</html>